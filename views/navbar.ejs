<nav class = "navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <a href="#" class="navbar-brand">Hello, Multiverse</a>
        </div>
        <div class="navbar-default">
            <ul class="navbar-nav nav">
                <% locals.app_modes.forEach(function(mode) { %>
                    <li <% if (locals.app_mode === mode) { %> 
                            class = "active" 
                        <% } %> > 
                        <a href="/mode/<%= mode %>"><%= mode %></a>
                    </li>
                <% }); %>
            </ul>
            <a type="button" href="/mode/play" class="btn btn-success navbar-right navbar-nav" style="text-align: center; margin-right: 0px; margin-top: 7px;">
                Play
            </a>
        </div>
    </div>
</nav>